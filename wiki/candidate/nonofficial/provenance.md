---
type: Reference
title: Non-official provenance map
description: Why this directory exists — which artifacts come from OpenWiki's own design and stay at the wiki root, and which exist only because agent-skills-repo's gates pin them, and therefore live here.
tags: [provenance, meta]
node_kind: RepoDoc
ingest_lane: concept
repo: local/agent-skills-repo
covers: [wiki-provenance, contract-paths, port-extensions]
libraries: []
generated_by: claude-code+opus (skill-bettor openwiki port)
generated_at: null
---

# Non-official provenance map

This wiki is produced by a host-native port of langchain-ai/OpenWiki's official `code` procedure, into a
repository that also treats `openwiki/` as a **typed contract surface**. Two different authorities decide
what exists here, and mixing them in one directory made the boundary invisible.

The split is now structural:

```text
openwiki/
  quickstart.md          <- OpenWiki's entrypoint
  architecture/  skill-assets/  validation/  governance/
  operations/    terminal-operator/  testing/  lifecycle/    <- OpenWiki's section shape
  index.md  (per directory)                                  <- OpenWiki's generated indexes
  .last-update.json                                          <- OpenWiki's run metadata
  nonofficial/           <- everything else, this directory
```

**Rule: the wiki root holds only what OpenWiki's own procedure produces. Anything else lives here.**

## Tier 1 — OpenWiki's own design (stays at the root)

| Artifact | Authority |
|---|---|
| `quickstart.md` | the official entrypoint; carries the task-routing table |
| the eight section directories | *"create one directory per major section, for example architecture/, workflows/, domain/, api/, data-models/, operations/, integrations/, testing/"* |
| every `index.md` | reserved OKF document, generated deterministically after each run — never hand-written |
| `.last-update.json` | run metadata (`updatedAt`, `command`, `gitHead`, `model`, `status`) |
| OKF v0.1 front matter on every concept page | `type` required; `title`, `description`, `tags` recommended |
| Mermaid diagrams on flow / lifecycle / data-model pages | official diagram discipline |

## Tier 2 — pinned by this repository's gates (lives here)

OpenWiki would not create any of these. They exist because
`scripts/check_openwiki.py` carries them as a hard-coded required-file list
(src: scripts/check_openwiki.py `"openwiki/nonofficial/usage.md",`),
and because `scripts/check_plan_package_compat.py`,
`scripts/check_lifecycle_datasets.py`, `scripts/check_autoresearch_lifecycle.py`,
`scripts/check_prompt_trace_assets.py`, `scripts/check_wiki_graph_sync.py` and
`tests/test_skill_asset_governance.py::test_skill_asset_structure`
(src: tests/test_skill_asset_governance.py `def test_skill_asset_structure() -> None:`)
name them literally — several also
require specific literal strings **inside** them
(src: scripts/check_openwiki.py `REQUIRED_LITERALS = {`).

| Path under `nonofficial/` | Why it is not OpenWiki's |
|---|---|
| `README.md` | OpenWiki's entrypoint is `quickstart.md`; this second entry exists only because `check_openwiki.py` pins it as a required file (src: scripts/check_openwiki.py `"openwiki/nonofficial/README.md",`) |
| `openwiki.yaml` | a page manifest — key/value pointers to the pinned pages, pinned in turn by the gate (src: scripts/check_openwiki.py `openwiki_version: openwiki@0.1.0`). Nothing in OpenWiki's own procedure emits such a file |
| `usage.md` | the command runbook |
| `asset-lifecycle-map.md` | |
| `stateful-workflow.md` | |
| `code-call-lifecycle.md` | required to contain measured values as literal strings |
| `production-bottlenecks.md` | |
| `autoresearch-composer-lifecycle.md` | required to contain `cases=4`, `passed=4`, `delta=1.0`, … |
| `prompt-trace-assets.md` | |
| `wiki-graph-sync-architecture.md` | required to contain `W3.license_unknown`, `GRAPH_DB_KIND=neo4j-http`, … |
| `schema-standards.md` | required to contain `WikiDocument`, `WikiEvent`, `EmbeddingMetadata`, … |
| `structured-lifecycle-data.md` | **generated by `scripts/render_lifecycle_openwiki.py` (src: scripts/render_lifecycle_openwiki.py `"structured-lifecycle-data.md"`) and byte-compared against a fresh render.** Not documentation at all — a build artifact that happens to be Markdown. Never hand-edit it |
| `provenance.md` | this page |

### How the relocation was done

Moving these required editing the gates, because they compare literal paths rather than scanning
directories. Every reference was repointed in one pass:

| File | What changed |
|---|---|
| `scripts/check_openwiki.py` | `REQUIRED_FILES` (src: scripts/check_openwiki.py `"openwiki/nonofficial/schema-standards.md",`), the `REQUIRED_LITERALS` keys, and the `openwiki.yaml` value literals |
| `scripts/check_plan_package_compat.py` | the required-path list (src: scripts/check_plan_package_compat.py `"openwiki/nonofficial/openwiki.yaml",`) |
| `scripts/check_wiki_graph_sync.py` | two required files (src: scripts/check_wiki_graph_sync.py `"openwiki/nonofficial/schema-standards.md",`) and the two pages it reads (src: scripts/check_wiki_graph_sync.py `"openwiki/nonofficial/wiki-graph-sync-architecture.md").read_text`) |
| `scripts/check_lifecycle_datasets.py` | the display-page path (src: scripts/check_lifecycle_datasets.py `"nonofficial" / "structured-lifecycle-data.md"`) |
| `scripts/check_autoresearch_lifecycle.py` | `REPORT` (src: scripts/check_autoresearch_lifecycle.py `REPORT = ROOT / "openwiki" / "nonofficial" / "autoresearch-composer-lifecycle.md"`) |
| `scripts/check_prompt_trace_assets.py` | the display-page path (src: scripts/check_prompt_trace_assets.py `"nonofficial" / "prompt-trace-assets.md"`) |
| `scripts/render_lifecycle_openwiki.py` | its write target **and** the dataflow line it emits (src: scripts/render_lifecycle_openwiki.py `"  -> openwiki/nonofficial/structured-lifecycle-data.md",`) — the second one matters, because the generated page is compared against the fresh render for exact equality (src: scripts/render_lifecycle_openwiki.py `if observed != output:`) and a stale dataflow line breaks it |
| `tests/test_skill_asset_governance.py` | the twelve required paths (src: tests/test_skill_asset_governance.py `"openwiki/nonofficial/openwiki.yaml",`) |
| `README.md` (repo root) | the two `openwiki entry` references |
| `nonofficial/openwiki.yaml` | its own self-referencing page values, each of which the gate pins verbatim (src: scripts/check_openwiki.py `structured_lifecycle_page: openwiki/nonofficial/structured-lifecycle-data.md`) |
| every wiki page | relative links repointed for the new depth |

`.github/workflows/wiki_graph_sync.yml` needed no change: its path filter is already recursive
(src: .github/workflows/wiki_graph_sync.yml `- 'openwiki/**/*.md'`), and
`scripts/sync_wiki_to_graph.py` walks the whole wiki tree with `rglob`
(src: scripts/sync_wiki_to_graph.py `wiki_root.rglob("*.md")`), so a page one directory deeper is
picked up by both without an edit.

## Tier 3 — skill-bettor port extensions

Present because of the *port*, not because of OpenWiki or of this repository.

| Extension | Where | Purpose |
|---|---|---|
| RepoDoc front-matter fields — `node_kind`, `ingest_lane`, `repo`, `covers`, `libraries`, `generated_by`, `generated_at` | every concept page | routes pages into skill-bettor's knowledge graph. OKF-legal producer extensions: the spec permits them and requires them to survive round trips |
| `generated_by: claude-code+opus (skill-bettor openwiki port)` | every concept page | records that the author was a host CLI session, not OpenWiki's Node agent |
| `.openwiki-review/` | **outside** `openwiki/` | subagent review transcripts. Deliberately outside the wiki tree — anything inside it becomes a section directory to the index generator |

## Rule for future additions

- Produced by OpenWiki's own procedure → the wiki root.
- Anything else → this directory, and add a row to Tier 2 or Tier 3 above.
- A gate that pins a new page must pin it **under `nonofficial/`**, never at the wiki root.
- If a Tier 2 entry is ever retired from its gate, it moves to the root only if OpenWiki's design
  actually calls for it; otherwise it stays here and its row records that it is no longer pinned.
